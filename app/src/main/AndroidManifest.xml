<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.senai.sc.tcc.candymanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.PrincipalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ClienteActivity" />
        <activity android:name=".activity.ListarPessoaActivity" />
        <activity android:name=".activity.ProdutoActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.MovimentoEstoqueActivity" />
        <activity android:name=".activity.PedidoActivity" />
        <activity android:name=".activity.PesquisaClienteActivity" />
        <activity android:name=".activity.ConfiguracaoActivity" />
        <activity android:name=".activity.PesquisaProdutoActivity" />
        <activity android:name=".activity.RelatorioActivity"></activity>
    </application>

</manifest>